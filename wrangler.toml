name = "imobiliaria-str"
compatibility_date = "2024-01-01"
pages_build_output_dir = ".next"

[build]
command = "npm run build"

[env.production]
vars = { NODE_ENV = "production" }
```

---

## **Arquivo `.gitignore` corrigido:**
```
.env
.env.local
.env*.local
node_modules
.next
.vercel
dist
build